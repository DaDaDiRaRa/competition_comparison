fastapi==0.115.12
uvicorn[standard]==0.34.0
httpx==0.28.1
python-multipart==0.0.20
pydantic==2.10.6
pymupdf==1.25.5
pillow==11.2.1
numpy==2.2.5
openpyxl==3.1.5
python-docx==1.2.0
rhwp-python==0.7.0
markdown==3.10.2

# MCP provider (/mcp) — C-1.
# ⚠ **1.12.4 로 묶인 이유는 두 겹이다**(실측으로 하나씩 걸렸다):
#    ① `pydantic==2.10.6`(이 파일) ↔ mcp>=1.15 는 `pydantic>=2.11` 을 요구한다.
#    ② `fastapi==0.115.12`(starlette<0.47) ↔ `sse-starlette>=3.4` 는 `starlette>=0.49.1` 을
#       **무조건** 요구한다. 3.0.3 은 그 요구가 `examples` extra 안에만 있어 공존한다.
#    핀을 안 걸면 clean 빌드는 pip 백트래킹으로 통과하지만, 기존 이미지에 out-of-band 설치가
#    끼면 **테스트는 전부 초록인데 앱이 안 뜬다** — 형제앱 arch-law-diagnose 가 그렇게 당했다.
# ⚠ **로컬 venv 도 이 버전으로 맞춰 둔다.** 로컬만 최신 mcp 면 `TransportSecuritySettings`
#    유무 같은 차이를 배포에서야 만난다. `tests/test_app_boot.py` 가 조건을 지킨다.
# ⚠ mcp 2.0.0 은 `mcp.server.fastmcp` 를 제거한다(plan-mcp-gateway §9).
mcp==1.12.4
sse-starlette==3.0.3
